@startuml

start

:Открыть Dropbox;

if (Есть аккаунт?) then (Да)
  :Ввести логин и пароль;
else (Нет)
  :Регистрация;
endif

:Вход в систему;

fork
  repeat
    :Синхронизация файлов;
    :Обновление на устройствах;
    :Отображение обновлённого состояния;
  repeat while (Система активна?)
fork again
  repeat
    :Пользователь выбирает действие;
    
    if (Загрузить файл?) then (Да)
      :Загрузка файла;
      :Отправка на сервер;
      :Сохранение в облаке;
    elseif (Скачать файл?) then (Да)
      :Скачивание файла;
    elseif (Удалить файл?) then (Да)
      :Удаление файла;
    elseif (Переместить файл?) then (Да)
      :Перемещение файла;
    elseif (Поделиться файлом?) then (Да)
      :Открытие общего доступа;
      :Получение ссылки;
    endif
  repeat while (Продолжить работу?)
end fork

:Выход из Dropbox;

stop

@enduml
